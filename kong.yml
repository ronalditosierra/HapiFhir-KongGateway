_format_version: "1.1"

services:
- name: hapi-fhir
  url: http://localhost:8080
  routes:
  - name: hapi-fhir-route
    paths:
    - /fhir
    strip_path: false

plugins:
- name: rate-limiting
  service: hapi-fhir
  config:
    minute: 5
    policy: local
    limit_by: ip

